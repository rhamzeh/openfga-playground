// SPDX-License-Identifier: Apache-2.0

/**
 * @deprecated Bundled samples are kept for offline/embedded use only.
 * At runtime the playground fetches the current sample list from
 * https://github.com/openfga/sample-stores via the GitHub API.
 * Use `loadSampleList` / `loadSample` from `@openfga/playground-core` instead.
 */
export const sampleAuthorizationModels: Record<string, unknown> = {};
